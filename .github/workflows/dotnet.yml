name: .NET

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "master" ]

env:
  BUILD_BUILDNUMBER: ${{ github.run_number }}


jobs:
  # build:

  #   runs-on: windows-latest

  #   steps:
  #   - uses: actions/checkout@v3
#     - name: Setup .NET 7
#       uses: actions/setup-dotnet@v3.0.3
#       with:
#         dotnet-version: 7.0.x
#     - name: Setup .NET 6
#       uses: actions/setup-dotnet@v3.0.3
#       with:
#         dotnet-version: 6.0.x
#     - name: Build Script
#       run: |
#         [System.Environment]::SetEnvironmentVariable('Version','${{ github.run_number }}')
#         . .\build.ps1 ; CIBuild

# #to do: publish test results, publish code coverage

#     - name: Push NuGet packages   
#       run: |
#         foreach($file in (Get-ChildItem ".\build\" -Recurse -Include *.nupkg)) {
#           dotnet nuget push $file --source "https://nuget.pkg.github.com/dmarttinen/index.json" --api-key ${{ secrets.PACKAGESAPI }}
#         }  


  dockerbuildandpush:
#    needs: build
    runs-on: 'ubuntu-latest'

    steps:
    - name: Download UI Package
      run: |
        dotnet nuget add source --username dmarttinen --password ${{ secrets.REGISTRYPWD }} --store-password-in-clear-text --name github "https://nuget.pkg.github.com/dmarttinen/index.json"
        mkdir -p built
        nuget install ChurchBulletin.UI -Version 1.0.0 -source "https://nuget.pkg.github.com/dmarttinen/index.json" -Outputdirectory built/ 
        shell: bash

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: onioncontainers.azurecr.io/my-image:1.0.0
        registry: onioncontainers.azurecr.io
        username: ${{ secrets.AZAPPID }}
        password: ${{ secrets.AZAPPKEY }}
          

